import XCTest
@testable import package_gitblame

final class package_gitblameTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(package_gitblame().text, "Hello, World!")
    }
}
